# Contributing

Open an issue before a large change. Keep pull requests focused and run the
checks before pushing.

```bash
pnpm install --frozen-lockfile
pnpm check
pnpm build
```

Never commit `.env` files or Cloudflare tokens.
